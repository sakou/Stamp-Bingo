# 開発環境用Dockerfile
FROM node:20-alpine

# 作業ディレクトリを設定
WORKDIR /app

# package.jsonとpackage-lock.jsonをコピー
COPY package*.json ./

# 依存関係をインストール
RUN npm ci

# アプリケーションコードをコピー
COPY . .

# Prismaクライアントを生成
RUN npx prisma generate

# ポート3000を公開
EXPOSE 3000

# 開発サーバーを起動（docker-compose.ymlでオーバーライド可能）
CMD ["npm", "run", "dev"]
